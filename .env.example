# If set, /api/scrape proxies to this Boltic-deployed Scrapling backend.
# Leave unset locally to spawn the Python sidecar instead (scraper/scrape.py).
SCRAPER_BACKEND_URL=https://scrappling-b092deec.serverless.boltic.app

# Optional: bearer token if you ever add auth on the Boltic side.
# SCRAPER_BACKEND_TOKEN=
